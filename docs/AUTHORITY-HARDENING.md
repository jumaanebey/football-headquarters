# Authority hardening — findings and completion matrix

Work package started 2026-09-10 from main `ea9a2b6` (PR #32 merged; `club-authority` v3 deployed). Companion to `docs/AUTHORITY-CONTRACTS.md`, `docs/AUTHORITY-RELIABILITY.md` and `docs/AUTHORITY-RECOVERY.md`. Status vocabulary: **verified already** (existing evidence cited), **fixed and tested**, **deployed and checked**, **blocked** (concrete dependency). Evidence types: deterministic tests through the real service on the SQL-equivalent store; live two-account evidence; browser viewport checks; none of these are physical-device tests.

## Tranche 1 — server entitlement and validation (PR: `claude/fhq-entitlement-guards`)

### Reproduced and fixed

| # | Finding | Before | After | Test |
| --- | --- | --- | --- | --- |
| 1 | `campus.apply` applied a layout's formation without the `formationUnlocked` gate that `formation.set` enforces. **Not a live exploit**: `formationUnlocked` returns true for every formation since the July 2026 decision, so both paths currently allow every scheme. | Two rules for one choice | One rule: `campus.apply` fails `locked` when the layout's formation is not unlocked; balances, revision and campus unchanged; allowed custom edits still apply | `tests/authorityEntitlements.test.ts` › 1 (policy mocked to lock a formation, plus the current-policy case) |
| 2 | Legacy admission accepted equipment levels above the Stadium level and hero levels above the Stadium training cap. The legacy client always enforced both, so only a hand-edited save could carry them, and only for accounts created before activation. | Admitted | `invalid_legacy`, no club created; honest boundary saves (slot level = Stadium level, hero at the cap) admitted | › 2 "a legacy save cannot smuggle…" |

### Inspected and passed (no change)

- `campus.apply` cannot add, drop, retype, duplicate or rename facilities/slots/gates, change wall counts, place on the edge or overlap; extra attributes such as a forged facility `level` are stripped by canonicalization and never applied.
- `gate.assign` requires an owned hero and an existing post; the defense snapshot drops any recorded assignment to a hero that is not owned.
- Equipment: locked slots, crown slots without purchase, levels above the Stadium and the cap are refused; the snapshot fields only entitled equipment.
- Facility upgrades stay behind the Stadium; hero training behind the Stadium cap; drills behind field level; starters cannot be "unlocked"; forged recruit ids are refused.
- Rival matches: a reservation is bound to its target; a second reservation is refused while one is open; a film produced against a different config is a `simulation_mismatch`, and the target receives no receipt.
- Defense consistency after edits: the snapshot captured at reservation equals the defender's local Test Defense snapshot (ids, buildings, guards); edits and upgrades made while the match is open do not change it; the settled result, the defender's receipt and the shared film all carry the attacked ids and replay to the same result; the next raid captures the new snapshot.
- Bounds at the parsing boundary: `1e999` (Infinity) in layout coordinates, `expectedRevision`, campaign stage and ticks is refused; fractional coordinates, 101 seen-ids, 121-char ids, 25-char names, extra choice fields, non-UUID ids, negative revisions and non-object bodies are refused with no state change. Film limits (1 501 commands, ticks > 1400, unordered ticks, unknown kinds/heroes/plans, off-field coordinates, extra/missing fields, non-hex hash, string script) are refused before any simulation; an altered-snapshot film is a mismatch; the worst valid film (1400 ticks) verifies well under five seconds in-process.
- Oversized legacy saves (> 450 KB) are refused before work.

### Deployment status

The reducer and admission changed, so the deployed function must be rebuilt and redeployed for the fixes to be live. Staged: readable `supabase/functions/club-authority/index.ts` (regenerated) and the minified artifact `supabase/recovery/club-authority.v4.min.js` (sha256 recorded in the PR). Deployment follows the pinned process in `docs/AUTHORITY-RECOVERY.md` and needs the owner's go-ahead; until then production runs v3, which has the two gaps above (both without a practical exploit).

## Tranche 2 — save ownership and compatibility (PR stacked on tranche 1)

### Fixed and tested

| # | Finding | Before | After | Test |
| --- | --- | --- | --- | --- |
| 8 | Signing into an account that already has a protected club adopted the server state on a device whose local save was a different (guest) club, and the autosave then overwrote that local club with the mirror. Enabling protection from the Settings button did back the local club up, but the tutorial and sign-in adoption paths did not. | Local guest club overwritten on adoption | `preserveLocalClub()` keeps the device's save as `fhq_backup_preprotect` (+ timestamp) before any adoption on a device that was not already mirroring that account, and before every `enable()` | `tests/authorityStorageFaults.test.ts` › preservation; call sites in `useAuthority.detect/enable` |

### Verified already / extended

- **6 Legacy save fixture matrix** (`tests/fixtures/legacySaves.ts`, `tests/saveFixtures.test.ts`): synthetic saves for the pre-fixed-base era (free-placed equipment, heroes without unlock fields), the fixed-base era, return progression + live defense cursor, custom campus, the protected mirror and a future client. Each loads with identity intact (name, currencies, trophies, roster ids/levels, heroes, facilities, campaign, history, equipment); pre-fixed-base equipment maps to slots and refunds once only when no unlocked slot exists; repeated load/migration is idempotent (excluding clock-driven regen, production, patrol positions and the daily/gauntlet day); truncated/negative/mistyped/mismatched saves throw `SaveLoadError` without touching the file or the last readable backup, and the existing `ErrorBoundary` offers restore-last-readable-backup, download-original and archive-before-fresh (no silent reset). No compensation or admission change.
- **7 Old-client write protection**: unknown fields written by a newer client survive the older loader and validator; a bootstrap carrying an old-shaped save for an account that already has a protected club returns the existing club unchanged (no downgrade path exists: browser roles have no privileges on the authority tables, only the pinned function writes them); film under other rules or with commands impossible under the current rules is refused explicitly (`invalid_film`/`simulation_mismatch`), never replayed under different rules. Unsupported-version fixture: `futureSave` + `CORRUPT_SAVES`. **Proposed, not built**: an explicit `stateVersion` on the authority club state would let the server refuse a downgrade *by an old function build*; today builds are pinned and deployed deliberately, so this is an owner decision.
- **8 Account-linking ownership** (`tests/accountOwnership.test.ts`): linking attaches credentials to the *current* uid (PUT), so the club, ledger and published base keep their owner; a rejected link changes nothing; signing into another account rebinds every authority request to the new owner; an answer that arrives after the account changed is discarded; sign-out never mints a new identity silently and leaves the local file alone. No merge policy was introduced.
- **9 Persistence failure recovery** (`tests/authorityStorageFaults.test.ts`): quota errors while recording send nothing and report `storage`; a read-blocked storage yields an empty ledger but fresh requests still work; malformed/foreign/corrupted persisted operations are ignored without erasing valid ones; a write interrupted at confirmation time keeps the last readable ledger and the reload retries safely (server dedupes); diagnostics storage failures never break an operation. Guest autosave failures still only log a warning (player-facing notice is Codex UI territory; recorded as a gap).

## Tranche 3 — time, economy, match lifecycle, history (PR stacked on tranche 2)

No code defects were reproduced in this tranche; every item closed on new executable evidence against the real service.

- **10 Time advancement parity** (`tests/authorityTime.test.ts`): for the same elapsed time the client's `advanceCampus` (with the server's UTC day) and the server's `settleClubState` produce identical energy, production, facility/hero levels, upgrade queues and day fields: a seven-day closed tab caps Energy and production and completes timed work; a hidden tab that catches up in one step equals many small steps; a clock that runs backwards leaves the client unchanged and makes the server refuse with `invalid_clock` (no state change); the UTC day boundary resets dailies/Gauntlet attempts exactly once on both sides; guest clubs keep the documented local-day reset.
- **11 Resource conservation** (`tests/authorityInvariants.test.ts`): a seeded 300-request random walk across every action type holds all invariants (finite, non-negative, Energy ≤ 100, production ≤ cap, level caps) and every accepted receipt accounts exactly for its balance change (Energy regen excepted), refused requests change nothing, and commits equal revisions; exact affordability (cost exactly → ok, one short → refused) for coins and Crowns; maximum levels (five stars, slot 10, parking 3, builders 5, three crown slots) refuse with `limit_reached`; Energy never exceeds 100 through rally, regen or a cancel refund; the campaign first-clear bonus and shards pay once across replays; the Gauntlet first-clear Crowns pay once per best; the daily sweep bonus pays once on the last claim. Prices unchanged.
- **12 Match lifecycle boundaries** (`tests/authorityLifecycle.test.ts`): begin/finish are allowed at the exact expiry instant and refused one millisecond later; an expired reserved game cancels with a refund, an expired started game cannot settle and cancels without one; an abandoned reservation blocks club changes until released and the release is idempotent; beginner protection lifts exactly at the third Season game for both sides; a shield ending at this instant no longer protects; kickoff drops the attacker's shield, a lost defense raises the defender's for two hours, a held defense earns a mastery hold; busy and missing targets are refused without an Energy charge; Energy is accounted once across reserve→cancel, reserve→begin→cancel, reserve→expire→cancel and reserve→finish.
- **13 Defense consequence and history integrity** (`tests/defenseHistory.test.ts`): thirty-five settled raids leave thirty newest-first receipts with every coin consequence applied exactly once at settlement (aged-out receipts keep their consequences); repeated status reads and reloads change nothing; marking seen touches only the flag; film for an aged-out match stays fetchable by both participants; two raids settled at the same millisecond are both kept in deterministic order. The legacy paginated inbox keeps its existing evidence (`tests/defenseInbox.test.ts`). Retention (30 receipts) unchanged.

## Tranche 4 — replay compatibility, determinism, workload bounds (PR stacked on tranche 3)

### Fixed and tested

| # | Finding | Before | After | Test |
| --- | --- | --- | --- | --- |
| 16 | `verifyMatch` copied the whole film into the battle result, so every settlement answer carried the film to the client (which never uses it) and every settled match stored the film twice (`result.replay` and `result.battleResult.replay`). | Settlement answer ≈ film size; film stored twice | Battle results never embed film; film is stored once and served only by the bounded `film` request | `tests/authorityWorkload.test.ts` (settlement answers, club state and status contain no `script`); v4 artifact regenerated |

### Verified / measured

- **14 Replay version compatibility** (`tests/replayCompatibility.test.ts`): supported film versions are v1 (legacy playback path only) and v2 under rules `hero-actions-3`; v0/v3, other rules and a missing rules field are refused; the modern replay path throws `Unsupported match rules` for anything else; incomplete v2 film (any of script/ticks/finalHash/snapshot/layout/heroes/specials/seed/plan missing) is refused; unsafe asset ids (remote URLs, `..`, data URIs) and unknown hero keys are refused; snapshots disagreeing with their film, embedded replay fields, shortened tick counts and mode swaps are refused or fail verification. Existing `tests/sharedCombat.test.ts` covers altered inputs, duplicate deployments and key reordering.
- **15 Determinism across execution paths**: a fixed eight-match corpus (`game/authority/matchCorpus.ts`, hashes committed in `tests/fixtures/determinism-corpus.json`) gives identical gameplay hashes and results through the Node headless engine, recorded-film replay and the authority verification path (`tests/determinism.test.ts`), and through **HeadlessChrome 152** via `npm run determinism:browser` (esbuild browser bundle, Playwright, 8/8 matches: `d24bc304 … 966a7756`). Presentation randomness (audio/fx draining) does not change the hash. Runtimes really exercised: Node v22.14.0 and Chrome 152 headless on this Mac; the deployed edge runtime was exercised by the live evidence in `docs/AUTHORITY-RECOVERY.md` (server-settled hashes equal to client hashes for every settled game). No physical device.
- **16 Workload bounds** (`tests/authorityWorkload.test.ts`, measured in-process): club state with 12 receipts ≈ 30 KB and grows by well under 600 B per receipt up to the 30-receipt cap; `status` answers stay far below the 450 KB request cap; a film answer ≈ 20–100 KB; verification of the longest corpus films (1 201 ticks) takes tens of milliseconds; the largest inputs the service parses are a 450 KB legacy save and a 1 500-command film. Settled matches accumulate one row each with no retention (`fhq_authority_matches`, indexed by owner/issued_at): **owner decision** whether to prune settled matches older than N days or beyond N per owner; not built because it is a data-retention policy.

## Tranche 5 — operational tooling, restore rehearsal, reconciliation (PR stacked on tranche 4)

### Reproduced and fixed

| # | Finding | Before | After | Test |
| --- | --- | --- | --- | --- |
| 18 | A twin tap (second tap while the same request was unconfirmed) is folded into one operation by the ledger, but its outcome comes back `confirmed, duplicate: true` and `protectedAction` / the match-finish handler ran the confirmation effects again: a second `track()` event (`building_upgrade`, `battle_confirmed`, …), a second celebration. Analytics itself has no retry (fire-and-forget), so this was the only retry-induced duplicate path. | Duplicate analytics event and effects on twin taps | Duplicate outcomes update the notice only; effects and events fire once | Ledger dedupe: `tests/authorityRecovery.test.ts` › duplicate tap; guards in `App.tsx` (`if (!outcome.duplicate)`) |
| 18 | `analytics.ts` read `(import.meta as any).env`, which the test runner's transform does not rewrite (only the direct `import.meta.env.X` form is), so the module was inert under tests and delivery had never been covered. Identical in the Vite build. | Untestable | Reads `import.meta.env` like `pvp.ts`; delivery covered against a test sink | `tests/analyticsDelivery.test.ts` |
| 21 | `art/encode-motion.mjs` (QB/Enforcer) wrote `game/heroMotionBounds.ts` from an empty object, so rerunning it would have erased the seven roster heroes' authored regions; `art/encode-roster-motion.mjs` merged correctly. Both encoders take their sources from a Codex image directory that is not in the repository, so neither was rerun. | Rerun drops seven heroes | Both encoders write through `art/motion-bounds-io.mjs`, which merges into the existing file and refuses non-integer boxes | `tests/heroAtlasTooling.test.ts` › motion bounds writer (temp file rerun keeps the others; both encoders import the shared writer) |

### Verified / built

- **17 Privacy-conscious diagnostics** (`game/online/diagnosticsExport.ts`, `tests/diagnosticsExport.test.ts`): the client's ring holds kind, short operation id, outcome, code, latency and revision — no tokens, payloads, saves or names (verified by inspection of every `record()` call). The export helper `buildAuthorityDiagnosticsReport` adds build stamp, rules version, availability, pending/failed/confirmed counts and the unfinished ledger entries **by short id and kind only**; every string passes through an identifier filter (`code()`), so a server message or token-shaped value becomes `redacted` at the source. The test seeds the ledger with a JWT-shaped string, a club name and a film and asserts none of it (nor `request`, `message`, the full account id) reaches the serialized report; corrupted diagnostics storage yields a valid report. **No Settings UI change** (Codex-owned): the helper is the tested serializer behind a future "copy diagnostics" control.
- **18 Analytics delivery** (`tests/analyticsDelivery.test.ts`, test sink = stubbed `fetch`; production `fhq_events` inserts are not inspectable from tests and were not exercised): tutorial → match start → settlement → upgrade events arrive once, in order, in one batch, under one player id, with the anon key only; the referrer is host-only; bursts flush at 25 per batch without loss or duplication; a sink failure is swallowed and never retried; with no destination configured nothing is queued. No error/performance telemetry destination exists in the client (only `ErrorBoundary` and image `onerror` handlers, none of which report), so there is nothing to verify there; adding one is a consent/policy decision and was not done.
- **19 Restore and rollback rehearsal** (`server/authoritySnapshot.ts`, `server/authorityRehearsal.ts`, `npm run authority:rehearsal`, `tests/restoreRehearsal.test.ts`): a synthetic world (two clubs, a settled campaign match, an open rival match, seven operation rows, a device with one confirmed action, one action whose answer was lost after the server applied it, and one never delivered) is exported in the SQL tables' column shapes, discarded, restored into a fresh memory store and checked: ownership of every club/match/operation, revisions, the active-match binding, byte-identical re-export. The device's replay then answers the lost request from the restored operation row (no commit), applies the undelivered one exactly once (revision +1), a second replay and a verbatim resend commit nothing, the restored open match settles once and a duplicate finish returns the stored receipt without a second defender entry, and restoring the backup again rolls everything back. The validator refuses 17 tampered/partial/inconsistent snapshot shapes. Isolated by construction (memory store, synthetic ids, no network); the procedure for a real restore is recorded in `docs/AUTHORITY-RECOVERY.md`.
- **20 Deployment/source parity guard** (`scripts/authority-parity.mjs`, `npm run authority:build -- --check`, record `supabase/recovery/deployed.json`, `tests/authorityParity.test.ts`): compares sources → generated `index.ts`, sources → fresh minified build → staged artifact (the documented `esbuild --minify --line-limit=220` packaging rebuilds byte-identically: v4 sha256 `58511f9b…` reproduced), the deployed artifact in the tree and at its pinned commit (`git show e5d8238:…` sha256 `9ebeed1b…`), the commit-pinned jsDelivr entry, and rules version drift between the deployed function and the sources; generated output is scanned for token-shaped strings. Exercised in a temporary git repository against a stale artifact, a stale `index.ts`, a tampered preserved artifact, a wrong record hash, a missing pinned commit, a rules-drift record, an unpinned entry, an incomplete record and a leaked token shape. On the real tree: all checks pass; the v3→v4 lag is reported as information (`--require-deployed-current` turns it into a failure for a release that needs the staged server). The live entry could not be re-read on 2026-09-10 06:20 UTC (management read timed out); `deployed.json` records the entry from the deploy call made from this session.
- **21 Artifact verification tooling** (`scripts/hero-atlas-lib.ts`, `npm run art:verify`, manifest `art/hero-atlas-manifest.json`): for all nine modern heroes, decodes the motion, elite, signature and (QB/Enforcer) reaction sheets and checks region count against the renderer's expected coverage (8×4 or 9×4 motion frames, hit flash from the reaction sheet on eight-column heroes, nine elite poses, four signature poses), every region inside the sheet and non-degenerate, row/column order matching `row * columns + column`, real foreground owned by every region after keying, overlap between authored boxes (warning), and signature/reaction registration for all four cells. Asset SHA-256s and a hash of the authored regions are recorded in the manifest so an unreviewed regeneration fails the check (`--write` after review with Codex). Result on the shipped art: 9 heroes, 33 sheets, no findings, no warnings. Nothing was regenerated.
- **22 One release verification command** (`npm run release:verify`): typecheck → tests → authority parity → hero atlases → restore rehearsal → production build (which runs the raster decode of every shipped asset via `prebuild`) → balance guard, then optional `--browser` (Chrome determinism corpus) and `--live` (two-account evidence and browser journey, owner-run, credentials never in the repository). Live checks are printed as **SKIPPED with the exact owner command** when not requested, and the summary states that deployed behavior is verified only by the recorded live evidence. Nothing is run twice. The CI workflow was not changed: this account's GitHub token lacks the `workflow` scope, so a workflow edit cannot be pushed; adding `npm run release:verify` (offline) to `.github/workflows/ci.yml` is a one-line follow-up for the owner. Measured: offline pass in 25 s with `--browser` on this Mac.

## Completion matrix (updated per tranche)

| Item | Status | Evidence / dependency |
| --- | --- | --- |
| 1 Campus/formation boundary | Fixed and tested (staged for v4) | `tests/authorityEntitlements.test.ts` › 1 |
| 2 Alternate entitlement routes | Fixed (admission bounds) and tested; other routes verified | › 2 |
| 3 Defense consistency after edits | Verified already + extended | › 3 |
| 4 Bounded input and execution | Verified already + extended | › 4 |
| 5 Reviewable result | This document + PR | — |
| 6 Legacy save fixture matrix | Verified already + fixtures added | `tests/saveFixtures.test.ts` |
| 7 Old-client write protection | Verified (no downgrade path) + tests; `stateVersion` proposal pending owner | `tests/saveFixtures.test.ts` › old-client |
| 8 Account-linking ownership | Fixed (local club preserved before adoption) and tested | `tests/accountOwnership.test.ts`, storage faults › preservation |
| 9 Persistence failure recovery | Verified + injection tests | `tests/authorityStorageFaults.test.ts` |
| 10 Time advancement parity | Verified (no defect) + tests | `tests/authorityTime.test.ts` |
| 11 Resource conservation | Verified (no defect) + invariant walk | `tests/authorityInvariants.test.ts` |
| 12 Match lifecycle boundaries | Verified (no defect) + boundary tests | `tests/authorityLifecycle.test.ts` |
| 13 Defense history integrity | Verified (no defect) + tests | `tests/defenseHistory.test.ts`, `tests/defenseInbox.test.ts` |
| 14 Replay version compatibility | Verified + tests | `tests/replayCompatibility.test.ts` |
| 15 Determinism across runtimes | Verified (Node, Chrome 152, edge via live evidence) | `tests/determinism.test.ts`, `npm run determinism:browser` |
| 16 Workload bounds | Fixed (film no longer duplicated in settlement answers/results; staged for v4) + measured; retention policy pending owner | `tests/authorityWorkload.test.ts` |
| 17 Privacy-conscious diagnostics | Verified (ring inspected) + export helper built and tested; Settings control deferred to Codex | `tests/diagnosticsExport.test.ts` |
| 18 Analytics delivery | Fixed (duplicate effects/events on twin taps; module inert under tests) and tested against a test sink; production sink not inspected | `tests/analyticsDelivery.test.ts`, `tests/authorityRecovery.test.ts` |
| 19 Restore and rollback rehearsal | Built, executed (`npm run authority:rehearsal`) and tested in isolation; never against production | `tests/restoreRehearsal.test.ts` |
| 20 Deployment/source parity guard | Built and tested against stale fixtures; passes on the real tree (deployment lagging: v4 staged) | `tests/authorityParity.test.ts`, `npm run authority:build -- --check` |
| 21 Artifact verification tooling | Fixed (QB/Enforcer encoder overwrote other heroes' bounds) + tooling built; shipped art verified 9/9 with no findings | `tests/heroAtlasTooling.test.ts`, `npm run art:verify` |
| 22 Release verification command | Built and run (offline + Chrome pass); CI workflow edit blocked by the token's missing `workflow` scope | `npm run release:verify` |
| 23 Evidence reconciliation | This matrix + the reconciliation and cleanup sections below | — |

## Reconciliation — implemented, deployed, verified (as of 2026-09-10, tranche 5)

| Area | Implemented (in git) | Deployed (production) | Verified |
| --- | --- | --- | --- |
| Server rules: formation gate on `campus.apply`, admission bounds on slot/hero levels (items 1–2) | Yes (PR #33) | **No** — production runs v3; the fix is in the staged v4 artifact | Deterministic service tests; **not** live |
| Server: film no longer duplicated in settlement answers/results (item 16) | Yes (PR #36) | **No** — staged v4 | `tests/authorityWorkload.test.ts`; **not** live |
| Client: local club preserved before adoption (item 8), duplicate-outcome guards (item 18), analytics env read (item 18), diagnostics export helper (item 17) | Yes (PRs #34, #39) | Not yet — ships with the next Vercel production deploy from main | Tests; no browser journey has been rerun since PR #31's (2026-09-10 04:55 UTC) |
| Verification-only items 3–4, 6–7, 9–15 | Tests and fixtures in git | n/a | Deterministic tests; items 10–15 also cross-runtime (Node, Chrome 152) and, for 15, the v3 live evidence |
| Tooling: parity guard, atlas verification, rehearsal, release command (items 19–22) | Yes (PR #39) | n/a | Executed on this Mac 2026-09-10; outputs recorded above |
| v4 deployment | Artifact `supabase/recovery/club-authority.v4.min.js` sha256 `58511f9b…` (rebuilds byte-identically from the sources) | **Awaiting owner authorization**; procedure in `docs/AUTHORITY-RECOVERY.md` | After deploy: `npm run authority:build -- --check --require-deployed-current`, then `npm run release:verify -- --live` (owner) and record the outputs |

**Blocked, with the concrete dependency**: v4 deployment (owner authorization); CI workflow edit (`gh auth refresh -s workflow`); a Settings "copy diagnostics" control (Codex coordination); settled-match retention and `stateVersion` downgrade refusal (owner policy decisions, items 7 and 16); physical-device and observed-player acceptance (remain open; no software evidence substitutes for them).

## Consolidated deferred QA cleanup inventory (no deletions performed)

Every account below was created for testing against project `ruzkpbvgzvqrrnexrffz`. None has been deleted or modified by this package; deletion remains the owner's deferred decision ("build everything else and come back to this later"). Where an id is unknown it is stated as unknown rather than guessed; those rows can be found by club name in `fhq_authority_clubs.state->>'teamName'` (protected clubs) or `fhq_bases` (legacy publish), and by `created_at` in `auth.users`.

| Created | Purpose | Account id | Club name | How to find |
| --- | --- | --- | --- | --- |
| before 2026-09-10 | earlier integrity QA (names `fhq-integrity-qa-…`) | `89bf29b7-09ba-4537-b09b-e000d2ac808d` | unknown | by id |
| before 2026-09-10 | earlier integrity QA | `41ba80f3-9809-498a-aee8-5633214b0a00` | unknown | by id |
| 2026-09-10 ~03:15 UTC | evidence run against crashing v2 (all requests 500) | `4c40cd9d-91be-45bd-9cb8-83087ec1f36e` (A), `f96fcf0e-4b73-4cf9-a6bc-d9a4ee1b305d` (B) | none (bootstrap never succeeded) | anonymous `auth.users` rows only |
| 2026-09-10 ~03:31 UTC | evidence run 1 against v3 (54/57) | `c5f960d1-cc2e-4e29-bf80-7ad2e4e460a4`, `bb5e10d0-cf14-4be2-9448-a45c95a116ca`, `7e275a6a-cd30-45b6-861a-bed148fd6c4a` | `fhq-authority-evidence-A/B/C` | by id or name |
| 2026-09-10 ~03:40 UTC | evidence run 2 against v3 (57/57) | `d38b8c11-9ee0-4b5d-8634-827a27737954` (A), `79d451a1-7033-4e28-8e0e-92b267270417` (B), `147c15d7-30f1-4fe2-b07c-4684c840c03c` (C) | `fhq-authority-evidence-A/B/C` | by id or name |
| 2026-09-10 ~04:25 UTC | browser journey, PR #29 preview (protection enabled from Settings) | **unknown** (the script does not print it) | the tutorial's default club name at the time; not recorded | `auth.users` anonymous row created ~04:25 UTC with a protected club at revision ≥ 3 |
| 2026-09-10 ~04:55 UTC | browser journey, PR #31 preview (automatic protection) | **unknown** (the script does not print it) | `Protected Preview FC` | by club name |
| 2026-09-10 ~05:07 UTC | Codex campus/gameplay live journey (local production preview) | **unknown** | `Campus QA FC` | by club name |

Future runs: `scripts/authority-evidence.ts` prints its account ids; `scripts/authority-browser-check.mjs` does not — printing the anonymous uid from the page's session would make the next browser account identifiable (small script change, not made here because the browser check was not rerun in this package). Analytics rows (`fhq_events`) for these sessions are keyed by the same ids and belong to the same deferred exclusion.


## Deployment reconciliation update — 2026-09-10 v4

PR #41 consolidated the stack and merged as e3f8669. The v4 server artifact is now ACTIVE with JWT verification enabled; management readback matches the committed artifact exactly. The 57-check live evidence suite passed; strict deployed-current parity passed. Items 1, 2 and 16 now have the staged server fixes deployed. See AUTHORITY-RECOVERY.md for artifact/rollback details and the three additional evidence account IDs. Copy diagnostics is implemented and browser-verified in the Codex follow-up, awaiting its client release. CI workflow credentials and retention/stateVersion policy remain separate outstanding items.

### Facility gameplay QA, 11 September 2026

- `e774b44e-e1d7-4664-b29b-4f6444d32fb7` — Facility Loop QA; v6 schedule/scouting/Stadium evidence, 41 checks passed. Deferred cleanup; not deleted. Excluded in scripts/qa-accounts.json. Full evidence: docs/FACILITY-GAMEPLAY.md.
